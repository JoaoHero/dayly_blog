/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ["i.postimg.cc", "avatars.githubusercontent.com", "github.com"]
    }
};

export default nextConfig;
